# Configurações do Servidor
PORT=3001
NODE_ENV=development

# Banco de Dados PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pdv_restaurante
DB_USER=pdv_user
DB_PASSWORD=MUDE_ESTA_SENHA

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT - IMPORTANTE: Gere uma chave segura para producao
JWT_SECRET=MUDE_ESTA_CHAVE_EM_PRODUCAO
JWT_EXPIRES_IN=8h

# Taxa de serviço (%)
TAXA_SERVICO=10

# CORS - URLs permitidas (separar por vírgula)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
